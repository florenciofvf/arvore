<?xml version="1.0" encoding="iso-8859-1"?>

<objeto titulo="Eleições" sub-icone="diamante" estrategia-parametro="PSMT_SET" xmlns:xi="http://www.w3.org/2001/XInclude">
	<arvore>SELECT concat(id, "-", descricao), id as inteiro FROM eleicao</arvore>
	<tabela>SELECT * FROM eleicao</tabela>

	<sub-tabela>SELECT * FROM eleicao WHERE id = </sub-tabela>
	<sub-update>UPDATE eleicao SET WHERE id = </sub-update>
	<sub-delete>DELETE FROM eleicao WHERE id = </sub-delete>
	<sub-insert>INSERT</sub-insert>
	<sub-observacao>OBSERVAÇÃO</sub-observacao>
	<sub-descricao>OBSERVAÇÃO</sub-descricao>

	<objeto titulo="Candidatos" sub-icone="pessoas" pesquisa-por-popup="true">
		<arvore>SELECT concat(id, "-", nome), id as inteiro FROM candidato WHERE eleicao_id = ?</arvore>
		<tabela>SELECT * FROM candidato WHERE eleicao_id = ?</tabela>
		<delete>DELETE FROM candidato WHERE eleicao_id = ?</delete>
		<sub-delete>DELETE FROM candidato WHERE id = </sub-delete>

		<xi:include href="enderecos.xml" />
	</objeto>
</objeto>