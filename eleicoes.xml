<?xml version="1.0" encoding="iso-8859-1"?>

<objeto titulo="Eleições" sub-icone="diamante" pesquisa-por-popup="false">
	<arvore>SELECT concat(id, "-", descricao), id as inteiro FROM eleicao</arvore>
	<tabela>SELECT * FROM eleicao</tabela>

	<objeto titulo="Candidatos" sub-icone="pessoas">
		<arvore>SELECT concat(id, "-", nome), id as inteiro FROM candidato WHERE eleicao_id = ?</arvore>
		<tabela>SELECT * FROM candidato WHERE eleicao_id = ?</tabela>
		<delete>DELETE FROM candidato WHERE eleicao_id = ?</delete>

		<objeto titulo="Endereços" icone="home">
			<arvore>SELECT concat(id, "-", logradouro), id as inteiro FROM endereco WHERE id_candidato = ?</arvore>
			<tabela>SELECT * FROM endereco WHERE id_candidato = ?</tabela>
			<delete>DELETE FROM endereco WHERE id_candidato = ?</delete>
		</objeto>
	</objeto>
</objeto>