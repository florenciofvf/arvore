<?xml version="1.0" encoding="iso-8859-1"?>

<objeto titulo="Eleição de Teste" icone="bola_verde" estrategia-parametro="PSMT_SET" xmlns:xi="http://www.w3.org/2001/XInclude">
	<xi:include href="eleicoes.xml" />

	<objeto titulo="Endereços" icone="home" item-icone="diamante" manter-vazio="true" icone-manter-vazio="home">
		<grupo-itens-na-arvore>SELECT concat(id, "-", logradouro), id as inteiro FROM endereco</grupo-itens-na-arvore>
		<grupo-itens-na-tabela>SELECT * FROM endereco</grupo-itens-na-tabela>

		<grupo-insert>INSERT INTO endereco(id_candidato, logradouro) VALUES(1, 'LOGRADOURO')</grupo-insert>
		<grupo-delete>DELETE FROM endereco WHERE id_candidato = ?</grupo-delete>

		<item-update>UPDATE endereco SET id_candidato = 1 WHERE id = 1</item-update>
		<item-tabela>SELECT * FROM endereco WHERE id = ?</item-tabela>
	</objeto>

	<objeto titulo="Auto Relacionamento">
		<grupo-itens-na-arvore>SELECT concat(id, "-", nome), id as inteiro FROM auto</grupo-itens-na-arvore>
		<grupo-itens-na-tabela>SELECT * FROM auto</grupo-itens-na-tabela>

		<objeto titulo="Anterior">
			<grupo-itens-na-arvore>
				SELECT concat(anterior.id, "-", anterior.nome), anterior.id as inteiro 
				FROM auto anterior, auto atual
				WHERE atual.id_auto is not null
				AND atual.id_auto = anterior.id
				AND atual.id = ?
			</grupo-itens-na-arvore>

			<item-objeto-titulo>anterior</item-objeto-titulo>

			<item-objeto-arvore>
				SELECT concat(anterior.id, "-", anterior.nome), anterior.id as inteiro 
				FROM auto anterior, auto atual
				WHERE atual.id_auto is not null
				AND atual.id_auto = anterior.id
				AND atual.id = ?
			</item-objeto-arvore>

			<item-objeto-tabela>
				SELECT anterior.*
				FROM auto anterior, auto atual
				WHERE atual.id_auto is not null
				AND atual.id_auto = anterior.id
				AND atual.id = ?
			</item-objeto-tabela>
		</objeto>

		<objeto titulo="Sucessor">
			<grupo-itens-na-arvore>SELECT concat(id, "-", nome), id as inteiro FROM auto WHERE id_auto = ?</grupo-itens-na-arvore>
			<item-objeto-titulo>sucessor</item-objeto-titulo>
			<item-objeto-arvore>SELECT concat(id, "-", nome), id as inteiro FROM auto WHERE id_auto = ?</item-objeto-arvore>
			<item-objeto-tabela>SELECT * FROM auto WHERE id_auto = ?</item-objeto-tabela>
		</objeto>
	</objeto>
</objeto>